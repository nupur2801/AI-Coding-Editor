# Plans

i am goign to build a terminal based code editor, which is build on react-ink and i have already installed ollama and `phi3:latest        4f2222927938    2.2 GB    4 months ago` this is the model i want to install you can use it using langchain

# i want certain features 
- `edit_files` : which cna edit any files, also update it, delete it etc
- `planner`: which can decide plans of what to do 

# loop forever unit the task is achived

for frontend i want to use ink-react basically i want a terminal based window where there should be one input and if i pass anything in that i can execute the command or showing me the prompt generaeation one by one instead of loading the entire chat of the text generated by the models

# you can keep the projects name codeing-editor and you can also apply that ascii style which is in gemini code or claude code

# you can take inspiration from claude code or genini cli of what to integrate how to keep the ui etc you can look into their github repo for inspiration

# generate codebase in the src folder C:\Users\Nupur\Desktop\coding_edtor\src and ii want o use nodejs and react do not use python

and all the trash will go into the trash folder keep everything super organize, create everything into multiple files like config.ts connector.ts etc follow the best coding practice and make it work

